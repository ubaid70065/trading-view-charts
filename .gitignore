# Credentials — never commit
.env
.env.*
!.env.example

# Cached instrument master (~33 MB, rebuilt on demand)
data/

node_modules/
.DS_Store
Thumbs.db
*.log

# Advanced Charts — licensed per user, not redistributable.
# Request access from TradingView and drop the folder in yourself.
public/charting_library/
